name: CRISBRAIN external distribution PAUSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Distribution paused
        run: echo "CRISBRAIN external distribution is paused."
